/** Section divider: the rule draws across, then the registration marks land. */
export default function Hairline() {
  return (
    <div className="container">
      <div className="rg-hair reveal" aria-hidden="true">
        <span className="rg-x rg-x--l"></span>
        <span className="rg-x rg-x--c"></span>
        <span className="rg-x rg-x--r"></span>
      </div>
    </div>
  );
}
